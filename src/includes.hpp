#ifndef INCLUDES_HPP
#define INCLUDES_HPP

// STL
#include <string>
#include <vector>

// SDL2 and SDL_gpu
#include <SDL2/SDL_gpu.h>

// GLM
#include <glm/glm.hpp>

// Using statements
using glm::vec2;
using std::vector;
using std::string;

// Type definitions
typedef uint8_t  byte;
typedef uint32_t uint;

#endif  // INCLUDES_HPP
